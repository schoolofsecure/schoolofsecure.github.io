# Academy mobile CTA fix

## Mit változtattunk
- Alsó CTA: `createPortal` → `document.body` (nem törik el a `#root` overflow miatt).
- Első CTA: trust blokk után (steps előtt).
- FAQ utáni CTA: mobilon full-width.
- Oldal alsó padding + cookie feljebb.
