# Mobile hamburger menu portal

## Mit változtattunk
- Mobilmenü `createPortal` → `document.body` (nem vágja le a `#root` overflow).
- Fixed pozíció a hamburger gomb alatt, z-index 60.
- Backdrop kattintásra bezár.
