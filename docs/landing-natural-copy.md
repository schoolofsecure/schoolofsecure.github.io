# Főoldal szöveg – természetesebb hangnem

# Főoldal kinézet visszaállítva

A Landing visszaállítva az eredeti `index.css` alapú dizájnra: `.hero`, `.features`, `.feat`, `.sidebar-layout`, terminál, Secure figura. Az új tartalom (Play/Learn, Iterali) megmaradt, a durva `section-block` dobozok eltávolítva.

