# English translation — task impl, styleConfig, UgyView

## Scope

User-facing Hungarian strings translated to natural English in:

- `src/pages/UgyView.jsx` — UI copy, Case terminology, `BrandLogo` instead of CM badge
- `src/tasks/styleConfig.js` — feedback template descriptions
- All 20 files in `src/tasks/impl/*.js` — narratives, hints, instructions, scenario text

## Branding

- CyberMystery → Iterali (`BrandLogo` in `UgyView`)
- Ügy → Case in `UgyView` UI strings

## Not in this pass

Other files may still show Hungarian (e.g. `ugyConfigs.jsx`, `TaskRenderer.jsx`, `styleHelper.js`, scoring messages).
