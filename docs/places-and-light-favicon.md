# Places wording + light favicon

## seats → places
- `AcademyCohortNote`: Only N **places** for the first cohort
- Academy trust list: N **places**

## Favicon
- Világos háttér (`#f4f7fb`)
- Csak kacsacsőrök: kék `<` + lila/kék `>` (nincs `i`)
- SVG elsődleges a `index.html`-ben
